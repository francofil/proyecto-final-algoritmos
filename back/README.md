# Backend - Planificador de Actividades con A*

Backend de la API para optimizar horarios de actividades usando el algoritmo A*.

## Instalación

```bash
pip install -r requirements.txt
```

## Ejecutar el servidor

```bash
uvicorn app:app --reload
```

El servidor estará disponible en `http://localhost:8000`

